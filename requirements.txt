# Quản lý Máy Chạy Thận — runtime + test deps
# Cài đặt: python -m pip install -r requirements.txt

# --- Desktop UI (main.py) ---
PyQt6>=6.5,<7

# --- Web API (server.py) ---
Flask>=3.0,<4
flask-cors>=4.0

# --- Excel I/O ---
openpyxl>=3.1,<4        # .xlsx (read + write)
xlrd>=2.0.1,<3          # .xls legacy — 2.0+ chỉ hỗ trợ .xls (bỏ .xlsx), đúng use-case

# --- PDF export (/api/ban-giao/export-pdf) ---
reportlab>=4.0,<5

# --- Tests ---
pytest>=8.0
